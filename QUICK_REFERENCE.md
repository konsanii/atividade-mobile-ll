# 🎯 Quick Reference Card - Consulta Rápida\n\n## Async/Await Syntax\n\n```javascript\n// Declarar função assíncrona\nasync function minhaFuncao() {\n  try {\n    // Operação que demora\n    const resultado = await fetch(url);\n    const dados = await resultado.json();\n    \n    return dados;\n    \n  } catch (erro) {\n    console.error(erro);\n  } finally {\n    // Sempre executa\n  }\n}\n\n// Chamar função\nconst resultado = await minhaFuncao();\n```\n\n## Fetch Pattern\n\n```javascript\nconst buscar = async (url) => {\n  const response = await fetch(url);\n  \n  if (!response.ok) {\n    throw new Error(`HTTP error! status: ${response.status}`);\n  }\n  \n  const dados = await response.json();\n  return dados;\n};\n```\n\n## AsyncStorage Pattern\n\n```javascript\n// Salvar\nawait AsyncStorage.setItem('chave', JSON.stringify(dados));\n\n// Buscar\nconst dados = await AsyncStorage.getItem('chave');\nconst objeto = JSON.parse(dados);\n\n// Deletar\nawait AsyncStorage.removeItem('chave');\n\n// Limpar tudo\nawait AsyncStorage.clear();\n```\n\n## Estado com Hooks\n\n```javascript\nconst [estado, setEstado] = useState(inicial);\nconst [carregando, setCarregando] = useState(false);\nconst [erro, setErro] = useState(null);\n```\n\n## Componentes React Native Básicos\n\n| Componente | Uso |\n|-----------|-----|\n| `<View>` | Container (div) |\n| `<Text>` | Texto |\n| `<TextInput>` | Input |\n| `<TouchableOpacity>` | Botão (clicável) |\n| `<ScrollView>` | Scroll vertical |\n| `<FlatList>` | Lista otimizada |\n| `<ActivityIndicator>` | Loading spinner |\n\n## Styling\n\n```javascript\nconst styles = StyleSheet.create({\n  container: {\n    flex: 1,\n    backgroundColor: '#fff',\n    paddingHorizontal: 20,\n  },\n  title: {\n    fontSize: 24,\n    fontWeight: 'bold',\n    color: '#333',\n    marginBottom: 10,\n  },\n});\n```\n\n## Alertas e Feedback\n\n```javascript\n// Alert simples\nAlert.alert('Título', 'Mensagem');\n\n// Alert com botões\nAlert.alert('Confirmar', 'Deletar?', [\n  { text: 'Cancelar' },\n  { text: 'OK', onPress: () => {} },\n]);\n\n// Loading\n<ActivityIndicator size=\"large\" color=\"#0066cc\" />\n```\n\n## Navegação (Tabs)\n\n```javascript\nimport { Tabs } from 'expo-router';\n\nexport default function Layout() {\n  return (\n    <Tabs>\n      <Tabs.Screen name=\"cadastro\" options={{ title: 'Cadastro' }} />\n      <Tabs.Screen name=\"lista\" options={{ title: 'Usuários' }} />\n    </Tabs>\n  );\n}\n```\n\n## FlatList Pattern\n\n```javascript\n<FlatList\n  data={dados}\n  renderItem={({ item }) => <Text>{item.nome}</Text>}\n  keyExtractor={(item) => item.id}\n  ListEmptyComponent={<Text>Vazio</Text>}\n/>\n```\n\n## Validação Básica\n\n```javascript\nif (!input.trim()) {\n  Alert.alert('Erro', 'Campo vazio');\n  return;\n}\n\nif (email.includes('@')) {\n  // Válido\n}\n```\n\n## Tratamento de Erros\n\n```javascript\n// Try/Catch/Finally\ntry {\n  // Tenta \n} catch (erro) {\n  // Pega erro\n  console.error(erro.message);\n} finally {\n  // Sempre executa\n  setCarregando(false);\n}\n```\n\n## RegEx Comum\n\n```javascript\n// Remover caracteres especiais\nconst limpo = cep.replace(/\\D/g, '');\n\n// Validar email\nconst validaEmail = (email) => {\n  return /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(email);\n};\n```\n\n## JSON Parsing\n\n```javascript\n// String para Objeto\nconst obj = JSON.parse(string);\n\n// Objeto para String\nconst str = JSON.stringify(obj);\n```\n\n## Datas\n\n```javascript\n// Data atual\nconst agora = new Date();\n\n// Formatado\nconst formatado = agora.toLocaleDateString('pt-BR');\n// Resultado: 01/04/2026\n```\n\n## IDs Únicos\n\n```javascript\nconst id = Date.now().toString();\nconst uuid = Math.random().toString(36).substr(2, 9);\n```\n\n## Console Debug\n\n```javascript\nconsole.log('Info:', dados);\nconsole.warn('Aviso:', alerta);\nconsole.error('Erro:', erro);\n```\n\n## Shortcuts Úteis\n\n| Tecla | Ação |\n|-------|------|\n| `a` | Abre Android |\n| `w` | Abre Web |\n| `r` | Recarrega |\n| `c` | Limpa console |\n| `m` | Mostra menu |\n\n## Comandos npm\n\n```bash\nnpm start          # Inicia Expo\nnpm run android    # Android específico\nnpm run web        # Web específico\nnpm run lint       # Verifica erros\nnpm install        # Instala dependências\n```\n\n## Imports Necessários\n\n```javascript\n// React\nimport React, { useState } from 'react';\n\n// React Native\nimport {\n  View, Text, TextInput, TouchableOpacity,\n  Alert, ScrollView, FlatList, StyleSheet\n} from 'react-native';\n\n// AsyncStorage\nimport AsyncStorage from '@react-native-async-storage/async-storage';\n\n// Expo Router\nimport { useFocusEffect } from '@react-navigation/native';\n```\n\n## Estrutura Padrão de Tela\n\n```javascript\nimport React, { useState } from 'react';\nimport { View, Text, FlatList, StyleSheet, Alert } from 'react-native';\n\nexport default function MinhaTelaScreen() {\n  const [dados, setDados] = useState([]);\n  const [carregando, setCarregando] = useState(false);\n\n  const carregarDados = async () => {\n    try {\n      setCarregando(true);\n      // lógica aqui\n    } catch (erro) {\n      Alert.alert('Erro', erro.message);\n    } finally {\n      setCarregando(false);\n    }\n  };\n\n  return (\n    <View style={styles.container}>\n      <Text style={styles.titulo}>Minha Tela</Text>\n      {/* conteúdo aqui */}\n    </View>\n  );\n}\n\nconst styles = StyleSheet.create({\n  container: { flex: 1, padding: 20 },\n  titulo: { fontSize: 24, fontWeight: 'bold' },\n});\n```\n\n## Checklist: O que verificar antes de submeter\n\n- [ ] App abre sem crashes\n- [ ] Todos os botões funcionam\n- [ ] Dados salvam e carregam\n- [ ] Tratamento de erros OK\n- [ ] Loading indicators mostram\n- [ ] Sem console errors\n- [ ] `npm run lint` passa\n- [ ] Código bem indentado\n- [ ] Variáveis bem nomeadas\n- [ ] Funções comentadas\n\n## Links Úteis\n\n- React Native Docs: https://react native.dev/\n- Expo Docs: https://docs.expo.dev/\n- ViaCEP API: https://viacep.com.br/\n- AsyncStorage: https://react-native-async-storage.github.io/\n\n---\n\n**Última atualização: 01/04/2026**\n"